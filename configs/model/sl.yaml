# @package _global_
model:
  algorithm: sl
  inner_epochs: 6
    # - 3
    # - 9
    # - 1.2
  rollout_batch_size: 100
  mini_batch_size: 2000
  num_starts: 100
  eval_per_agent: True
  eval_multistep: True
  loss: list
  listnet_alpha: 0.0
  lookback_intervals: 0


callbacks:
#   # batch_size_finder:
#   #   _target_: marlprp.utils.callbacks.RolloutBatchSizeFinder
#   #   mode: "power"
#   #   steps_per_trial: 1
#   #   init_val: 2
#   #   max_trials: 25
#   #   batch_arg_name: train_batch_size

  validation_scheduler:
    _target_: marlprp.utils.callbacks.ValidationScheduler
    warmup_epochs_n: 1
    reset_policy_after_warmup: true
